"""Motor de vulnerabilidades (absorbido de Patch Genius, adaptado a SQLite).

Paquete regular explícito y sin side effects: cada módulo se importa derecho
(`from src.vuln.schema import ensure_schema`).
"""
